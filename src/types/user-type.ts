export interface IUserAuthData {
  isAuth: boolean;
  email: string;
  token: string;
  uid: string;
}

export interface IUserData {
  userId: string;
  name: string;
  surname: string;
  avatar: string;
}
